import { HStack, Typography, VStack } from "@/components/elements";

function TxFrameIndex() {
  return (
    <VStack padding={4}>
      <HStack>
        <Typography variant="h3">hi this is a tx frame</Typography>
      </HStack>
    </VStack>
  );
}

export { TxFrameIndex };
